# Legacy Python Reference

The Python implementation is retained as reference/prototype code only.

Active development now lives in the root Go module under `cmd/` and `internal/`.
The Python scripts are not part of the normal build or test flow.
